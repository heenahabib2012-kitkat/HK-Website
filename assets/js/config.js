/* ==========================================================================
   HK Business Consultancy: site configuration (edit here)
   --------------------------------------------------------------------------
   formEndpoint  URL that accepts a POST of the consultation form (FormData),
                 e.g. a Formspree, Basin or Getform endpoint, or your own API.
                 While empty, submitting the form opens the visitor's email
                 app with the enquiry pre-filled and addressed to `email`.
   email         Address that receives enquiries in the email fallback.
   whatsapp      WhatsApp number in international format, digits only.
   ========================================================================== */
window.HK_CONFIG = {
  formEndpoint: '',
  email: 'info@honestandkeen.com',
  whatsapp: '971585815229'
};
